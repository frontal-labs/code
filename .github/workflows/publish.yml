name: Publish SDKs and CLI

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  id-token: write

concurrency:
  group: publish-${{ github.ref }}
  cancel-in-progress: false

env:
  CARGO_TERM_COLOR: always
  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
  NODE_TOKEN: ${{ secrets.GITHUB_TOKEN }}

jobs:
  # ── Check changeset status ────────────────────────────────────
  changeset-status:
    name: changeset status
    runs-on: ubuntu-latest
    outputs:
      hasChangesets: ${{ steps.changeset.outputs.hasChangesets }}
    steps:
      - uses: actions/checkout@v4
      - uses: changesets/action@v1
        id: changeset
        with:
          publish: npx changeset publish
          version: npx changeset version
          check: npx changeset status --since origin/main

  # ── Build TypeScript SDK ──────────────────────────────────────
  typescript:
    name: typescript sdk (build + test)
    needs: [changeset-status]
    if: needs.changeset-status.outputs.hasChangesets == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://registry.npmjs.org"

      - name: Install workspace dependencies
        run: bun install

      - name: Biome check
        run: bunx @biomejs/biome check sdks/typescript

      - name: Build SDK
        run: bun run --filter @frontal-labs/frontal-code-sdk build

      - name: Test SDK
        run: bun run --filter @frontal-labs/frontal-code-sdk test

  # ── Build Rust SDK ────────────────────────────────────────────
  rust:
    name: rust sdk (fmt + clippy + test)
    needs: [changeset-status]
    if: needs.changeset-status.outputs.hasChangesets == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install pinned Rust toolchain
        run: rustup show

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: . -> target

      - name: Check formatting
        run: cargo fmt -p frontal-code-sdk -- --check

      - name: Clippy
        run: cargo clippy -p frontal-code-sdk --all-targets -- -D warnings

      - name: Test
        run: cargo test -p frontal-code-sdk

  # ── Build CLI ─────────────────────────────────────────────────
  cli:
    name: cli (build)
    needs: [changeset-status]
    if: needs.changeset-status.outputs.hasChangesets == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install workspace dependencies
        run: bun install

      - name: Build TypeScript SDK (CLI dependency)
        run: bun run --filter @frontal-labs/frontal-code-sdk build

      - name: Verify CLI package
        working-directory: cli
        run: ./scripts/verify.sh

  # ── Publish all packages ──────────────────────────────────────
  publish:
    name: publish packages
    needs: [typescript, rust, cli]
    if: needs.changeset-status.outputs.hasChangesets == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          registry-url: "https://npm.pkg.github.com"

      - name: Install workspace dependencies
        run: bun install

      - name: Build TypeScript SDK
        run: bun run --filter @frontal-labs/frontal-code-sdk build

      - name: Publish TypeScript SDK to GitHub Packages (npm)
        working-directory: sdks/typescript
        run: npm publish --access public --registry https://npm.pkg.github.com
        env:
          NODE_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish TypeScript SDK to npmjs.org
        working-directory: sdks/typescript
        run: npm publish --access public --registry https://registry.npmjs.org
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish CLI to GitHub Packages (npm)
        working-directory: cli
        run: npm publish --access public --registry https://npm.pkg.github.com
        env:
          NODE_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Publish Rust SDK to crates.io
        working-directory: sdks/rust
        run: cargo publish --token "${{ secrets.CARGO_REGISTRY_TOKEN }}"
        env:
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push SDK image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: sdks/Dockerfile
          push: true
          tags: |
            ghcr.io/${{ github.repository }}/frontal-code-sdk:latest
            ghcr.io/${{ github.repository }}/frontal-code-sdk:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Run changeset publish
        run: npx changeset publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
