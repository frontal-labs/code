package(default_visibility = ["//visibility:public"])

filegroup(
    name = "srcs",
    srcs = glob(
        ["**/*.rs"],
        exclude = ["**/target/**"],
    ) + ["Cargo.toml"],
)

filegroup(
    name = "test_srcs",
    srcs = glob(["tests/**/*.rs"]),
)

sh_test(
    name = "test",
    srcs = ["//tools:crate.sh"],
    data = ["//:cargo_workspace"],
    args = [
        "frontal-code-runtime",
        "test",
    ],
    tags = [
        "rust",
        "test",
    ],
)

sh_test(
    name = "fmt",
    size = "small",
    srcs = ["//tools:crate.sh"],
    data = ["//:cargo_workspace"],
    args = [
        "frontal-code-runtime",
        "fmt-check",
    ],
    tags = [
        "fmt",
        "rust",
    ],
)

sh_test(
    name = "lint",
    srcs = ["//tools:crate.sh"],
    data = ["//:cargo_workspace"],
    args = [
        "frontal-code-runtime",
        "clippy",
    ],
    tags = [
        "lint",
        "rust",
    ],
)
