package(default_visibility = ["//visibility:public"])

filegroup(
    name = "srcs",
    srcs = glob(
        ["**/*.rs"],
        exclude = ["**/target/**"],
    ) + ["Cargo.toml"],
)

filegroup(
    name = "test_srcs",
    srcs = glob(["tests/**/*.rs"]),
)

sh_test(
    name = "test",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-sandbox",
        "test",
    ],
    tags = [
        "rust",
        "test",
    ],
)

sh_test(
    name = "fmt",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-sandbox",
        "fmt-check",
    ],
    tags = [
        "fmt",
        "rust",
    ],
)

sh_test(
    name = "lint",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-sandbox",
        "clippy",
    ],
    tags = [
        "lint",
        "rust",
    ],
)
