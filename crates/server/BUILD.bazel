package(default_visibility = ["//visibility:public"])

filegroup(
    name = "srcs",
    srcs = glob(
        ["**/*.rs"],
        exclude = ["**/target/**"],
    ) + ["Cargo.toml"],
)

sh_binary(
    name = "frontal-code-server",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-server",
        "run",
    ],
)

sh_test(
    name = "test",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-server",
        "test",
    ],
    tags = [
        "rust",
        "test",
    ],
)

sh_test(
    name = "fmt",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-server",
        "fmt-check",
    ],
    tags = [
        "fmt",
        "rust",
    ],
)

sh_test(
    name = "lint",
    srcs = ["//tools:crate.sh"],
    args = [
        "frontal-code-server",
        "clippy",
    ],
    tags = [
        "lint",
        "rust",
    ],
)
