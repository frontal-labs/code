[package]
name = "frontal-code-server"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true

[[bin]]
name = "frontal-code-server"
path = "src/main.rs"

[dependencies]
axum = { version = "0.7", features = ["ws"] }
futures-util = "0.3"
frontal-code-events = { path = "../events" }
frontal-code-integrations = { path = "../integrations" }
frontal-code-orchestrator = { path = "../orchestrator" }
frontal-code-repo = { path = "../repo" }
frontal-code-runtime = { path = "../runtime" }
frontal-code-tools = { path = "../tools" }
serde = { version = "1", features = ["derive"] }
serde_json.workspace = true
tokio = { version = "1", features = ["macros", "net", "rt-multi-thread", "sync"] }
hmac = "0.12"
sha2 = "0.10"
hex = "0.4"
reqwest = { version = "0.12", default-features = false, features = ["json", "rustls-tls"] }

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }

[lints]
workspace = true
