services:
  frontal-code-server:
    environment:
      FRONTAL_CODE_SERVER_LANE_TRANSPORT: ${FRONTAL_CODE_SERVER_LANE_TRANSPORT:-local-docker}
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
