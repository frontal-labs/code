apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: frontal-code

resources:
  - namespace.yaml
  - configmap.yaml
  - persistentvolumeclaims.yaml
  - frontal-code-server.yaml
  - frontal-code-slack.yaml
  - ingress.yaml

generatorOptions:
  disableNameSuffixHash: true
