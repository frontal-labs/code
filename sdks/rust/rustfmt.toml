# Rust formatting for the Frontal Code Rust SDK (frontal-code-sdk).
# Resolved by `cargo fmt` for files under sdks/rust.
edition = "2021"
max_width = 100
tab_spaces = 2
newline_style = "Unix"
use_small_heuristics = "Default"
